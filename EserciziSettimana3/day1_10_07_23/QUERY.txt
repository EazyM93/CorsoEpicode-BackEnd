// GENERARE I DATI DENTRO LE TABELLE


// clienti (lo stesso ripetuto per tutte le altre tabelle con i relativi dati)

INSERT INTO public.clienti(numero_cliente, nome, cognome, data_nascita, regione_residenza)
VALUES (1, 'Manuel', 'Centini', 1993, 'Lazio')


// Estrarre il nome e il cognome dei clienti nati nel 1982

SELECT nome, cognome FROM public.clienti
WHERE data_nascita = 1982


// Estrarre il numero delle fatture con iva 20%

SELECT COUNT(*) FROM public.fattura
WHERE iva = 20


// Riportare il numero di fatture e la somma dei relativi importi divisi per anno di fatturazione

SELECT COUNT(*), data_fattura, SUM(importo)  FROM public.fattura
GROUP BY data_fattura
ORDER BY data_fattura


// Estrarre i prodotti attivati nel 2017 e che sono in produzione oppure in commercio

SELECT descrizione FROM public.prodotti
WHERE data_attivazione = 2017 AND (in_produzione != 0 OR in_commercio !=0)


// Considerando soltanto le fatture con iva al 20%, estrarre il numeri di fatture per ogni anno

SELECT COUNT(*), data_fattura FROM public.fattura
WHERE iva = 20
GROUP BY data_fattura


// Estrarre gli anni in cui sono state registrate più di 2 fatture con tipologia 